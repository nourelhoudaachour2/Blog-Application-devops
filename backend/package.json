{
  "name": "blog-backend",
  "version": "1.0.0",
  "scripts": {
    "start": "node index.js",
    "test": "echo 'Tests OK' && exit 0",
    "lint": "eslint . --ext .js || true"
  },
  "dependencies": {
    "express": "^4.18.0",
    "prom-client": "^14.0.0"
  },
  "devDependencies": {
    "eslint": "^8.0.0"
  }
}
